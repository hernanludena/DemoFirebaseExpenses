- Creamos project en FireStore Console
Colocamos el applicationId = "com.example.demofirebase" obtenido de build.gradle.kts

- Get SHA1 fingerprint certificate in Android Studio in MAC
keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android 
Se obtiene el SHA1 desde linea de comandos y se copia dentro de Firebase Console
y luego se copia el archivo google-services.json en este directorio

Tools/Firebare
- Add Analytics
- ADD Cloud Firestore
 
- En Firestore database inicializamos el servicio de Firestore
Create Database / Star test mode